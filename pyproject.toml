[project]
name = "burnout-guard"
version = "0.1.0"
description = "耗尽卫士 - 认知负荷监测与干预调度系统"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "httpx>=0.27.0",
]
